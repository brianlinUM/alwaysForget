whois <address>	// Look up for who owns the address (i.e. www.google.com)


-------------------------------------------------------------------------------
Rust stuff

cargo build // compiles project
cargo build --release // compiles with optimizations
cargo run // compiles (if modified) and executes project
cargo check // check if project is compilable without actually compiling it.
